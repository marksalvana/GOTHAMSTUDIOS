{%- if template.name == 'cart' -%}{{ 'customer.order.subtotal' | t }}&nbsp;{%- endif -%}
{%- if settings.currency_show_codes -%}
  {{- cart.total_price | money_with_currency -}}
{%- else -%}
  {{- cart.total_price | money -}}
{%- endif %}
